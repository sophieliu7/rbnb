<div class="container">




  <div class="tool-container">
    <% if @tool.photo.url.nil? %>
      <% url = cl_image_tag("qkdcyx2j5vajhsqdsd1o.jpg", crop: :fill) %>
    <% else %>
      <% url = cl_image_path(@tool.photo, crop: :fill) %>
    <% end %>

      <div class="tool-picture text-center" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.2)), url('<% url %>')" >
      </div>

    <div class="tool-content">
      <div class="tool-title">
        <img class="tool-user" src="https://kitt.lewagon.com/placeholder/users/tgenaitay">
        <h1><%= @tool.name.capitalize %></h1>
        <h4><%= @tool.place %></h4>
      </div>
      <div class="tool-description">
        <p><%= @tool.description %></p>
      </div>
      <div class="tool-form">
        <h2><%= @tool.price_per_day %>€ per day</h2>
      </div>
    </div>

  </div>


 <!-- <%= @tool.name %>
  <br>
  <%= @tool.price_per_day %>
  <br>
  <%= @tool.description %>
  <br>
  <%= @tool.place %>
  <br>
  <%= @tool.user.first_name %>
</div> -->
